{
  "$comment": "How skills compose into plugins — the single source of truth for the build. Edit this, then `npm run build` (regenerates plugins/ and all manifests; `npm test` fails on drift). Per plugin: `version` drives harness update/caching, so bump it for every plugin whose generated plugins/<name>/ dir changes in a PR (including via a bundled dependency); `dependencies` are bundled into the plugin's self-contained dir; `requires` is documented-only (user installs it separately); `tools` pins the CLI tools its skills/hooks invoke — canonical sources write `<name>@x.y.z` and the build substitutes the pin.",
  "marketplace": {
    "name": "lightcone-research",
    "displayName": "Lightcone Research Skills",
    "description": "Agent skills for Lightcone Research — ASTRA authoring and validation.",
    "repo": "LightconeResearch/agent-skills",
    "owner": {
      "name": "Lightcone Research",
      "email": "info@lightconeresearch.org"
    }
  },
  "plugins": [
    {
      "name": "astra",
      "version": "0.0.3",
      "description": "Reference for the ASTRA specification (astra.yaml): top-level structure, sub-analyses, inputs/outputs, decisions and options, prior insights and findings, evidence and quote verification, and composition mechanics. Invoke when reading, writing, validating, or debugging an astra.yaml spec, or when the user asks about ASTRA schema or sub-analysis composition. Ships hooks that validate astra.yaml on save and remind agents to load the skill when they read an ASTRA file.",
      "skills": [
        "astra"
      ],
      "hooks": "hooks/astra/hooks.json",
      "tools": {
        "$comment": "Pins for the CLI tools this plugin's skills and hooks invoke via `uvx <name>@<version>`. Canonical skills/ and hooks/ write the literal `@x.y.z` placeholder — never a concrete version — and `npm run build` substitutes each bundling plugin's pin into the generated plugins/<name>/ copies.",
        "astra-tools": "0.2.13"
      },
      "dependencies": []
    },
    {
      "name": "lightcone",
      "version": "0.0.3",
      "description": "Companion for working on a Lightcone project — an ASTRA analysis (astra.yaml) executed with the `lc` CLI. Covers the project model (a uv project, direct or containerized, stored as a DataLad dataset), the development workflow (probe with `lc run`, integrate recipes, commit, `lc materialize`), status interpretation (current/behind/stale) and failure diagnosis, publishing the RO-Crate view, and how to engage the user at each phase: interview to scope a brand-new project, orient and summarize when resuming an existing one. Invoke whenever the user wants to start, resume, plan, run, debug, or discuss a Lightcone/ASTRA analysis project. Bundles the `astra` plugin (spec reference, validate-on-save & session-start hooks) — install `lightcone` alone to get the full stack.",
      "skills": [
        "lightcone"
      ],
      "hooks": "hooks/lightcone/hooks.json",
      "tools": {
        "$comment": "Unlike astra-tools, lightcone-cli is NOT run through uvx: the user installs it once with `uv tool install lightcone-cli`, which also puts git-annex on PATH for their own git commands in a project. So this pin is a MINIMUM VERSION, not an invocation pin — the SessionStart hook and the skill compare `lc --version` against it. It names the first release carrying the rebuilt CLI (LightconeResearch/lightcone-cli#190); until that release is cut, an installed 0.4.x correctly reports as too old.",
        "lightcone-cli": "0.5.0"
      },
      "dependencies": [
        "astra"
      ]
    }
  ]
}
