{"config":{"lang":["en"],"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"","level":1,"title":"Lightcone Research Agent Skills","text":"<p>Portable, open-standard skills that teach coding agents the Lightcone Research stack.</p> <p>The skills follow the Agent Skills standard, work across Claude Code, Codex and OpenCode, and are packaged as plugins with their hooks included.</p> <p>GitHub View on GitHub</p>","path":["Documentation","Home"],"tags":[]},{"location":"#plugins","level":2,"title":"Plugins","text":"Plugin What it provides <code>astra</code> ASTRA authoring guidance and validation hooks. <code>lightcone</code> Companion for a whole Lightcone project — scoping, the <code>lc</code> execution workflow, publishing. Bundles <code>astra</code>, so installing it alone gives the full stack.","path":["Documentation","Home"],"tags":[]},{"location":"#install","level":2,"title":"Install","text":"<p>The only prerequisite is <code>uv</code>: plugins with Python dependencies install them on first use.</p> Claude CodeClaude AppCodex CLICodex AppOpenCode <pre><code>claude plugin marketplace add LightconeResearch/agent-skills\nclaude plugin install astra@lightcone-research\n</code></pre> <p>Then invoke <code>/astra:astra</code> in a session.</p> <p>Go to Customize → Plugins, click Add, then choose Add marketplace → Add from repo. Paste <code>https://github.com/LightconeResearch/agent-skills</code>, pick <code>astra</code>, and call it with <code>/astra:astra</code>.</p> <pre><code>codex plugin marketplace add LightconeResearch/agent-skills\ncodex plugin add astra@lightcone-research\n</code></pre> <p>Then invoke the skill in Codex, for example <code>$astra:astra</code>.</p> <p>Click the arrow beside Create and open Plugins. Install the <code>LightconeResearch/agent-skills</code> marketplace, search for <code>astra</code>, and install it. Then call <code>/astra:astra</code>.</p> <p>OpenCode reads Agent Skills directly and loads plugins as JavaScript files, so there is no marketplace step: install the skills with the <code>skills</code> CLI, then drop the generated hooks file into OpenCode's plugin directory.</p> <pre><code>npx skills add https://github.com/LightconeResearch/agent-skills/tree/main/plugins/astra -a opencode -g\nmkdir -p ~/.config/opencode/plugins\ncurl -fsSL https://raw.githubusercontent.com/LightconeResearch/agent-skills/main/plugins/astra/opencode/astra.js -o ~/.config/opencode/plugins/astra.js\n</code></pre> <p>The skill lands in <code>~/.agents/skills/</code> (drop <code>-g</code> for <code>./.agents/skills/</code>); the agent loads it when the task matches, or when you ask it to \"use the astra skill\". See the OpenCode page for what the plugin file does.</p>","path":["Documentation","Home"],"tags":[]},{"location":"#tutorial","level":2,"title":"Tutorial","text":"<p>New to ASTRA? Get started by working through a complete analysis — fitting ΛCDM to 580 Type Ia supernovae — with an agent doing the work and ASTRA keeping the record.</p>","path":["Documentation","Home"],"tags":[]},{"location":"astra/","level":1,"title":"ASTRA","text":"<p>The <code>astra</code> plugin helps an agent write and revise an <code>astra.yaml</code>: a machine-readable record of an analysis's inputs, outputs, and consequential methodological decisions.</p> <p>The skill focuses on the judgment that is hard to capture in a schema alone, including what should count as a decision, how choices should be parameterized in code, and when an analysis should be split into smaller units.</p>","path":["Documentation","ASTRA"],"tags":[]},{"location":"astra/#what-the-plugin-ships","level":2,"title":"What the plugin ships","text":"<ul> <li>The <code>astra</code> skill —   the authoring guidance itself. Once the plugin is installed, invoke it as   <code>/astra:astra</code> in Claude or <code>$astra:astra</code> in Codex; agents also load it on   their own when they work with an ASTRA file.</li> <li>A validate-on-save hook — whenever the agent saves an ASTRA file, the   spec is re-validated and the result is pushed straight back to the agent.</li> <li>A session-start hook — starting a session in a directory that contains   an <code>astra.yaml</code> orients the agent with the spec's location and the   analysis's shape.</li> </ul>","path":["Documentation","ASTRA"],"tags":[]},{"location":"astra/#install","level":2,"title":"Install","text":"<p>The only prerequisite is <code>uv</code>: the plugin runs the ASTRA CLI through <code>uvx</code>, which installs everything else it needs on first use.</p> Claude CodeClaude AppCodex CLICodex AppOpenCode <pre><code>claude plugin marketplace add LightconeResearch/agent-skills\nclaude plugin install astra@lightcone-research\n</code></pre> <p>Go to Customize → Plugins, click Add, then choose Add marketplace → Add from repo. Paste <code>https://github.com/LightconeResearch/agent-skills</code>, pick <code>astra</code>, and call it with <code>/astra:astra</code>.</p> <pre><code>codex plugin marketplace add LightconeResearch/agent-skills\ncodex plugin add astra@lightcone-research\n</code></pre> <p>Then invoke the skill in Codex, for example <code>$astra:astra</code>.</p> <p>Click the arrow beside Create and open Plugins. Install the <code>LightconeResearch/agent-skills</code> marketplace, search for <code>astra</code>, and install it. Then call <code>/astra:astra</code>.</p> <p>OpenCode reads Agent Skills directly and loads plugins as JavaScript files, so there is no marketplace step: install the skills with the <code>skills</code> CLI, then drop the generated hooks file into OpenCode's plugin directory.</p> <pre><code>npx skills add https://github.com/LightconeResearch/agent-skills/tree/main/plugins/astra -a opencode -g\nmkdir -p ~/.config/opencode/plugins\ncurl -fsSL https://raw.githubusercontent.com/LightconeResearch/agent-skills/main/plugins/astra/opencode/astra.js -o ~/.config/opencode/plugins/astra.js\n</code></pre> <p>The skill lands in <code>~/.agents/skills/</code> (drop <code>-g</code> for <code>./.agents/skills/</code>); the agent loads it when the task matches, or when you ask it to \"use the astra skill\". See the OpenCode page for what the plugin file does.</p>","path":["Documentation","ASTRA"],"tags":[]},{"location":"astra/#try-it","level":2,"title":"Try it","text":"<p>The best introduction is the getting started tutorial: it fits the standard cosmological model to 580 supernovae, with an agent doing the work and ASTRA keeping the record.</p>","path":["Documentation","ASTRA"],"tags":[]},{"location":"astra/#going-further","level":2,"title":"Going further","text":"<p>The full ASTRA documentation lives at astra-spec.org — the format reference, the schema, and a ground-up getting started guide.</p>","path":["Documentation","ASTRA"],"tags":[]},{"location":"getting-started/","level":1,"title":"Getting started with ASTRA","text":"<p>This tutorial gets you started with ASTRA — the specification for recording an analysis's inputs, outputs, and methodological decisions — and the <code>astra</code> plugin, which teaches your agent to work with it. The full specification lives at astra-spec.org.</p> <p>We work through a classic cosmology problem: using the relation between the brightness and redshifts of supernovae to estimate the dark energy content of the universe. We will fit the standard ΛCDM model of cosmology to 580 Type Ia supernovae from Suzuki et al. 2012. Mechanically, this is a straightforward least-squares fit with a single free parameter, Ω_Λ.</p>","path":["Tutorials","Getting started with ASTRA"],"tags":[]},{"location":"getting-started/#what-you-need","level":2,"title":"What you need","text":"<p>Two things on your PATH:</p> Tool Why Install <code>uv</code> Runs Python and the ASTRA CLI astral.sh/uv Claude Code, Codex or OpenCode The agent doing the work <code>npm install -g @anthropic-ai/claude-code</code> / <code>npm install -g @openai/codex</code> / opencode.ai <p>Then the <code>astra</code> plugin, which teaches your agent the format:</p> Claude CodeCodexOpenCode <pre><code>claude plugin marketplace add LightconeResearch/agent-skills\nclaude plugin install astra@lightcone-research\n</code></pre> <pre><code>codex plugin marketplace add LightconeResearch/agent-skills\ncodex plugin add astra@lightcone-research\n</code></pre> <pre><code>npx skills add https://github.com/LightconeResearch/agent-skills/tree/main/plugins/astra -a opencode -g\nmkdir -p ~/.config/opencode/plugins\ncurl -fsSL https://raw.githubusercontent.com/LightconeResearch/agent-skills/main/plugins/astra/opencode/astra.js -o ~/.config/opencode/plugins/astra.js\n</code></pre> <p>The first line installs the skill, the rest the plugin's hooks — see the OpenCode page.</p> <p>There is nothing else to install: the plugin runs the ASTRA CLI through <code>uvx</code>, which fetches it on first use.</p> <p>We recommend that you let your agent do the work from here on out — the tutorial gives you copy-pasteable prompts.</p> <p>Make a directory to work in, and start your agent in it:</p> <pre><code>mkdir sn-cosmology &amp;&amp; cd sn-cosmology\n</code></pre> <p>Then start your agent.</p> <p>Everything in a <code>text</code> block below is a prompt: paste it into the agent, not into your shell. Shell commands appear only where you are meant to look at something yourself.</p> <p>The rest of the tutorial is intentionally sparse — we leave your own agent to do some of the explaining for us.</p>","path":["Tutorials","Getting started with ASTRA"],"tags":[]},{"location":"getting-started/#1-the-data","level":2,"title":"1. The data","text":"<pre><code>Download the Union2.1 supernova compilation from the Supernova Cosmology\nProject into a data/ directory:\n\n  https://supernova.lbl.gov/Union/figures/SCPUnion2.1_mu_vs_z.txt\n\nThen show me the first few rows and tell me what's in the file.\n</code></pre>","path":["Tutorials","Getting started with ASTRA"],"tags":[]},{"location":"getting-started/#2-the-spec","level":2,"title":"2. The spec","text":"<p>Say what the analysis is:</p> <pre><code>Use the astra skill to set up an astra.yaml for this. I want to fit a flat\nLCDM model to this data to recover Omega_Lambda, varying only Omega_Lambda\nand using the metadata in the file's header.\n\nTwo outputs: the best fit, and a downstream best-fit Hubble diagram figure\nwith an absolute panel and a residuals panel.\n\nIn this first pass, let's only use inputs, outputs, and recipes. Please walk\nme through each element that you add.\n</code></pre> <p>When the agent writes <code>astra.yaml</code>, a hook fires and validates it immediately. You should see something like this:</p> <pre><code>version: \"0.0.12\"\nname: \"Cosmic expansion from Type Ia supernovae\"\n\ninputs:\n  - id: union21\n    type: data\n    source: data/SCPUnion2.1_mu_vs_z.txt\n    description: &gt;\n      Union2.1 SN Ia compilation. 580 rows spanning z = 0.015 to 1.414. The\n      header gives the assumed absolute magnitude, M(h=0.7), so the distance\n      moduli carry an h = 0.7 calibration.\n\noutputs:\n  - id: best_fit\n    type: metric\n    description: &gt;\n      Best-fit Omega_Lambda with its uncertainty, chi-squared, dof. H0 is held\n      at 70 km/s/Mpc to match the calibration in the data file's header, not\n      fitted.\n    inputs: [union21]\n    recipe:\n      command: python src/fit.py --data {inputs.union21} --out {output}\n\n  - id: hubble_diagram\n    type: figure\n    description: &gt;\n      Two panels sharing a redshift axis: distance modulus with the best-fit\n      curve, and residuals below it.\n    inputs: [union21, best_fit]\n    recipe:\n      command: &gt;\n        python src/plot_hubble.py --data {inputs.union21} --fit\n        {inputs.best_fit} --out {output}\n</code></pre>","path":["Tutorials","Getting started with ASTRA"],"tags":[]},{"location":"getting-started/#3-run-it","level":2,"title":"3. Run it","text":"<p>ASTRA does not run anything: it validates and inspects. The choice of runner stays yours, and here the runner is the agent.</p> <pre><code>Use uv to set up a virtual environment and install any needed packages.\n\nThen implement the analysis. Write the scripts the recipes call, run them, and\nshow me the Hubble diagram.\n</code></pre> <p>Where results go</p> <p>ASTRA's <code>{output}</code> is a placeholder; because nothing in ASTRA executes recipes, nothing in ASTRA decides where output lands either. That is yours to choose. <code>results/&lt;output_id&gt;</code> is a good default.</p> <p>One number comes back: Ω_Λ = 0.722 ± 0.013.</p> <p></p>","path":["Tutorials","Getting started with ASTRA"],"tags":[]},{"location":"getting-started/#4-check-it-against-the-paper","level":2,"title":"4. Check it against the paper","text":"<p>Is our result consistent with that found in the original paper? We check it by quoting them — and ASTRA verifies the quote against the paper itself.</p> <pre><code>The paper that published this catalogue is Suzuki et al. 2012, arXiv\n1105.3470. Cache the paper, verify their own Omega_Lambda from these\nsupernovae, and show me the output. Then record it in astra.yaml as a prior\ninsight with the quote as evidence, and tell me how our constraint compares to\ntheirs.\n</code></pre> <p>The text is searched in the cached PDF; running <code>astra paper verify-quote</code> gives:</p> <pre><code>✓ Verified   Quote verified on page(s) [17]\n</code></pre> <pre><code>prior_insights:\n  suzuki_2012_result:\n    label: \"Union2.1's own dark-energy constraint\"\n    claim: &gt;\n      Suzuki et al. (2012) constrain the dark energy density from these\n      supernovae alone, in a flat universe, to Omega_Lambda = 0.705\n      (+0.040, -0.043) including systematic errors.\n    created_at: \"2026-07-27T00:00:00Z\"\n    tags: [physics, priors]\n    evidence:\n      - id: suzuki_2012_sne_alone_flat_lcdm\n        doi: \"10.48550/arXiv.1105.3470\"\n        quote:\n          exact: \"In a flat Universe, SNe Ia alone constrain the dark-energy density, ΩΛ, to be ΩΛ = 0.705+0.040−0.043 including systematics\"\n        location:\n          value: \"page=17\"\n</code></pre> <p>If you want to run the check yourself:</p> <pre><code>uvx astra-tools validate astra.yaml --verify-evidence\n</code></pre> <pre><code>✓ Schema validation passed\n✓ Semantic validation passed\n\nVerifying evidence...\n✓ Evidence (prior_insights): 1/1 verified\n</code></pre> <p>Our central value agrees with theirs, but look at the error bars!</p>","path":["Tutorials","Getting started with ASTRA"],"tags":[]},{"location":"getting-started/#5-add-a-decision","level":2,"title":"5. Add a decision","text":"<p>Ours are three times tighter — 0.722 ± 0.013 against their 0.705 ± 0.04, on the same 580 supernovae. Find out why:</p> <pre><code>Let's understand why our error bars are so much tighter than theirs. Download\nthe two Union2.1 covariance files:\n\n  https://supernova.lbl.gov/Union/figures/SCPUnion2.1_covmat_nosys.txt\n  https://supernova.lbl.gov/Union/figures/SCPUnion2.1_covmat_sys.txt\n\nWork out which one the main data file's error column is using, then add a\ndecision to astra.yaml to switch to the other, and see what impact it has on\nour constraint.\n</code></pre> <p>The error column matches the no-systematics file: our fit has been statistical only, and everything the systematic covariance knows was sitting in a file we had not opened. Switching to it gives:</p> Ω_Λ ours, statistical 0.722 ± 0.013 ours, statistical + systematic 0.714 ± 0.030 Suzuki et al., published 0.705 (+0.040, −0.043) <p>The value barely moves. The uncertainty more than doubles — and only then is it comparable with the paper's, which includes systematics too. Our second row agrees with theirs to 0.2σ: their data, their systematics, their method.</p> <p>A comparison of best-fit numbers would have ranked this decision as nearly irrelevant. It is the difference between a number that resembles theirs and a number you can put beside theirs.</p>","path":["Tutorials","Getting started with ASTRA"],"tags":[]},{"location":"getting-started/#going-further","level":2,"title":"Going further","text":"<p>Three more decisions are sitting in this analysis, none of them implemented here:</p> <ul> <li>Optimiser — likely a null result, and worth recording as one.</li> <li>Redshift range — the high-z supernovae carry the longest lever arm and the   worst systematics.</li> <li>Dark energy model — assume w = −1, or fit it. The weaker assumption, at   the cost of a strong degeneracy with Ω_Λ.</li> </ul> <p>Each is one more option, one more recipe argument, and one more reason written down.</p> <p>The <code>astra</code> plugin page covers what the plugin ships, and the full ASTRA documentation lives at astra-spec.org — the format reference, the schema, and a ground-up getting started guide.</p>","path":["Tutorials","Getting started with ASTRA"],"tags":[]},{"location":"opencode/","level":1,"title":"OpenCode","text":"<p>OpenCode reads skills in the open Agent Skills format directly, and it has no plugin marketplace and no <code>hooks.json</code>. So a Lightcone plugin installs on OpenCode in two parts: the skills through the <code>skills</code> CLI, and the hooks as a single OpenCode plugin file that this repository generates from the same hook scripts Claude Code and Codex run.</p>","path":["Documentation","OpenCode"],"tags":[]},{"location":"opencode/#install","level":2,"title":"Install","text":"<p>The only prerequisite is <code>uv</code>, as on every harness. The examples install <code>astra</code>; substitute <code>lightcone</code> for the full stack — its directory ships both skills and its plugin file includes <code>astra</code>'s hooks, so do not install both.</p> <p>1. Skills — with the <code>skills</code> CLI, pointed at the packaged plugin directory:</p> <pre><code>npx skills add https://github.com/LightconeResearch/agent-skills/tree/main/plugins/astra -a opencode -g\n</code></pre> <p>This installs into <code>~/.agents/skills/</code>, one of the directories OpenCode scans. Drop <code>-g</code> to install into the current project (<code>./.agents/skills/</code>) instead.</p> <p>2. Hooks — one JavaScript file, copied into OpenCode's plugin directory:</p> <pre><code>mkdir -p ~/.config/opencode/plugins\ncurl -fsSL https://raw.githubusercontent.com/LightconeResearch/agent-skills/main/plugins/astra/opencode/astra.js \\\n  -o ~/.config/opencode/plugins/astra.js\n</code></pre> <p>Use <code>.opencode/plugins/</code> inside a project for a per-project install. Restart OpenCode; the plugin loads at startup.</p>","path":["Documentation","OpenCode"],"tags":[]},{"location":"opencode/#what-the-plugin-does","level":2,"title":"What the plugin does","text":"<p><code>plugins/&lt;name&gt;/opencode/&lt;name&gt;.js</code> is generated by <code>npm run build</code> from the plugin's <code>hooks.json</code>. It embeds the hook scripts verbatim — tool versions already pinned — and maps the hook events onto OpenCode's plugin API:</p> <code>hooks.json</code> event OpenCode hook Effect <code>PostToolUse</code> (<code>Write\\|Edit\\|apply_patch</code>) <code>tool.execute.after</code> on <code>write</code>, <code>edit</code>, <code>patch</code> Whenever a save touches <code>astra.yaml</code>, the spec is re-validated and the report is appended to the tool result the model reads. <code>SessionStart</code> <code>experimental.chat.system.transform</code> (pre-warmed on <code>session.created</code>) The session primer — spec location and analysis shape, and for <code>lightcone</code> the state of the <code>lc</code> engine — runs once per session and is added to the system prompt on every request. <p>Nothing else is installed. The file needs <code>bash</code> on PATH; the scripts need <code>uvx</code> and tell the agent when it is missing, exactly as on the other harnesses.</p> <p>Experimental hook</p> <p>The session primer relies on <code>experimental.chat.system.transform</code>, which OpenCode marks experimental. If a release renames it, validate-on-save keeps working and only the primer goes quiet — please open an issue.</p>","path":["Documentation","OpenCode"],"tags":[]},{"location":"opencode/#using-the-skills","level":2,"title":"Using the skills","text":"<p>OpenCode does not namespace skills by plugin: the agent loads <code>astra</code> or <code>lightcone</code> through its <code>skill</code> tool when the task matches the description, and you can simply ask it to \"use the astra skill\".</p>","path":["Documentation","OpenCode"],"tags":[]},{"location":"opencode/#differences-from-claude-code-and-codex","level":2,"title":"Differences from Claude Code and Codex","text":"<ul> <li>No marketplace. Updates are a re-run of the two install commands.</li> <li>Interactive or not? The <code>lightcone</code> primer asks the user before   installing or upgrading <code>lc</code> when a person may be present. OpenCode does not   expose how it was launched, so the primer always asks — in a headless   <code>opencode run</code> the agent will report instead of acting.</li> <li>Uninstall by deleting <code>~/.agents/skills/&lt;skill&gt;/</code> and   <code>~/.config/opencode/plugins/&lt;plugin&gt;.js</code>.</li> </ul>","path":["Documentation","OpenCode"],"tags":[]},{"location":"opencode/#try-it","level":2,"title":"Try it","text":"<p>The getting started tutorial works on OpenCode as it does elsewhere: the validate-on-save hook fires when the agent writes the spec.</p>","path":["Documentation","OpenCode"],"tags":[]}]}