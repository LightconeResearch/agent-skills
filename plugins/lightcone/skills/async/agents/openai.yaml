interface:
  display_name: "Prepare Lightcone Async Jobs"
  short_description: "Estimate and classify asynchronous jobs"
  default_prompt: "Use $async to prepare and classify this Lightcone job for synchronous or asynchronous execution."
